---
description: 用户消息含 bystep 时只讲解步骤与代码、不直接编辑仓库
alwaysApply: true
---

# Bystep 模式（仅当用户说 bystep 时生效）

**触发**：当前用户消息里出现 `bystep`（不区分大小写）。**若未出现 `bystep`，忽略本文件其余全部要求**。

## 必须遵守

1. **不要**使用编辑类工具改仓库：不写文件、不跑会改工作区的命令。
2. **要**用清晰步骤告诉用户**自己**怎么改：顺序编号、每步改哪个文件、改什么、为什么。
3. **要**给出**可复制的正确代码**或完整片段，并标明位置（相对仓库根路径 + 行号）。
4. 若需展示「改前/改后」对比，用 Markdown 代码块；引用现有代码仍用可跳转的 ` ```startLine:endLine:path ` 格式。

## 禁止

- 代用户执行修改或假装已改好仓库。
- 只给概念不落代码（除非用户只要概念）。
