---
description: 回答格式规范：结论优先、公式用纯文本
alwaysApply: true
---

# 回答格式规范

## 结论优先（理解核对类问题）

当用户用自己的话描述调用链、语法理解、架构流程并问「对不对」「是不是」时：

1. **第一句必须明确结论**：用「对」「不对」「基本对，但有一点要改」等可判定的表述。
2. **第二段再展开**：纠正点、完整链路、代码位置、易错点等。

避免先把长解释写完才在末尾轻描淡写结论。

## 公式与计算：用纯文本，不用 LaTeX

Cursor 对话里 LaTeX（`\( \)`、`\[ \]`、`$$` 等）不会渲染，用户会看到原始命令。

- 用纯文本写清楚，例如：`4 个 worker × (pool_size + max_overflow) ≈ 40 条连接`
- 可用 Unicode：`× ± ≤ ≥ ≠`；用反引号包伪公式，例如 `pool_size + max_overflow`
- 不要在聊天里写 LaTeX
