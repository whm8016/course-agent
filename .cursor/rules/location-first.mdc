---
description: 涉及文件、函数、配置项或「在哪改」时，第一步必须先给出可定位位置
alwaysApply: true
---

# 位置优先（回答与说明顺序）

当回复涉及**具体文件、函数、类、配置项、环境变量或「去哪里改」**时（含用户骂娘也要照做）：

1. **第一条信息必须是可定位位置**，在展开解释之前写出，二选一或组合：
   - **文件**：相对仓库根目录的路径 + **行号范围**（与当前版本一致），例如 `backend/config.py` **第 118 行** 或 **113–118 行**。
   - **若适合用代码引用块**：单独一行的围栏 ` ```118:118:backend/config.py `（Cursor 可一键跳转）。
2. **再写**该处做什么、怎么改、和别处的区别。

## 可跳转代码引用（必须遵守）

引用**仓库内已有代码**、且希望用户在 Cursor 里**一点即跳**时：

- **路径**：只用**相对仓库根目录**的正斜杠路径，例如 `backend/api/lightrag.py`、`frontend/src/App.tsx`。
- **禁止**：在围栏信息行里写 **Windows/macOS 绝对路径**（如 `c:\Users\...\file.py`、`/home/.../file.py`）。聊天里这类引用**经常无法跳转**，用户已多次反馈。
- **围栏格式**：第一行且**单独成行**：` ```起始行:结束行:relative/path.ext `，下一行起才是代码内容；**不要**和列表符号写在同一行。
- **新建/示意代码**（尚不存在于仓库）：可用普通 ` ```语言 ` 代码块；不要冒充 `line:line:path` 引用格式。

## 不要求位置的情况

- 纯概念题、与仓库无关的通识说明；
- 尚未在代码库中定位到具体位置时，先说明「未在仓库中找到」，再写下一步怎么搜。

## 与其他规则的关系

- 若同时满足「调用链 / 代码链」类问题，仍遵守 `code-chain-locations.mdc`，且**本条优先保证「第一句/第一块」就是路径+行号**（链路的每一步也都要带位置）。
