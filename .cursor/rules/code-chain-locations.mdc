---
description: 解释调用链/代码链时必须给出可跳转的具体文件与行号
alwaysApply: true
---

# 代码链说明必须带位置

当用户询问**调用链、代码链、数据流、请求从入口到内部的走向**（含「哪里用到」「谁调谁」「SSE/API 链路」等）时，回答必须包含**可定位的代码位置**，不能只写模块名或函数名。

## 必须提供

1. **文件路径**：**可跳转引用必须用相对仓库根目录路径**（例如 `backend/api/chat.py`）。说明文字里若要写本机绝对路径可以另写，但**代码引用围栏的信息行不得只用绝对路径**（否则 Cursor 聊天里常无法点击跳转）。
2. **行号范围**：关键步骤用 **起始行–结束行** 标出（与仓库当前版本一致）。
3. **引用格式**：优先使用 Cursor 可一键跳转的代码引用块（单独一行的围栏）：

```startLine:endLine:relative/path/to/file.py
//仅展示关键几行或省略号 …
```

- 围栏的 ``` 必须单独成行；不要用列表项与 ``` 同一行。
- **`relative/path` 约定**：从打开的工作区 / 仓库根算起，用正斜杠；**不要**写 `c:\...` 或 `file:///...` 作为围栏里的 path。
- 若行号过多，可按「链路步骤」分段引用，每一步至少一处 `path + 行号`。

## 建议写法

- 用有序列表按**时间顺序**写链路，每一步：`作用简述` → `文件:行号` →（可选）短引用。
- 若涉及多个入口（例如 `/api/chat` 与 `/api/chat/lightrag`），分节写清，避免混成一条链。

## 不要求

- 不必贴出整个文件；只贴与链路相关的片段。
- 不必为显而易见的标准库调用标行号，但**项目内**从入口到下游的每一跳都要有位置。
