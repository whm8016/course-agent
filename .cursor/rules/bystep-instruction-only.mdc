---
description: 用户消息含 bystep 时只讲解步骤与代码、不直接编辑仓库
alwaysApply: true
---

# Bystep 模式（仅当用户说 bystep 时生效）

**触发**：当前用户消息里出现 `bystep`（不区分大小写，如 `Bystep`、`BYSTEP`）。**若未出现 `bystep`，忽略本文件其余全部要求**（避免干扰普通对话与自动改代码）。

## 必须遵守

1. **不要**使用编辑类工具改仓库：不写文件、不跑会改工作区的命令（含补丁、重命名、安装改 lockfile 等只读可保留；以「是否改动用户代码/配置」为准）。
2. **要**用清晰步骤告诉用户**自己**怎么改：顺序编号、每步改哪个文件、改什么、为什么。
3. **要**给出**可复制的正确代码**或完整片段，并标明**位置**（相对仓库根路径 + 行号/锚点，与现有 `location-first` 规则一致）。
4. 若需展示「改前/改后」对比，用 Markdown 代码块；引用现有代码时仍用可跳转的 ` ```startLine:endLine:path ` 格式。

## 禁止

- 代用户执行修改或假装已改好仓库。
- 在 bystep 模式下只给概念不落代码（除非用户只要概念）。

## 与 Agent 能力的关系

用户说 `bystep` 时，助手处于「教练/文档作者」模式；用户想恢复自动改代码时，**不要**带 `bystep`，或明确说「你直接改」等。
