---
description: 涉及文件/函数/代码链时必须给出可跳转的位置引用
alwaysApply: true
---

# 代码引用与位置规范

## 位置优先

当回复涉及**具体文件、函数、类、配置项、环境变量或「去哪里改」**时：

1. **第一条信息必须是可定位位置**，在展开解释之前写出。
2. **再写**该处做什么、怎么改。

## 可跳转代码引用格式（仓库内已有代码）

- 格式：` ```起始行:结束行:相对路径 ` 单独成行，下一行起写代码内容
- **路径**：只用相对仓库根目录的正斜杠路径，例如 `backend/api/chat.py`
- **禁止**：围栏信息行里写 Windows/macOS 绝对路径（`c:\Users\...`、`/home/...`）——Cursor 里无法跳转
- **禁止**：把开头 ``` 与列表符号写在同一行
- **新建/示意代码**（不存在于仓库）：用普通 ` ```语言 ` 块，不要冒充 `line:line:path` 格式

## 代码链 / 调用链说明

当用户询问调用链、数据流、「谁调谁」时：

- 不能只写模块名或函数名，必须带文件路径 + 行号范围
- 用有序列表按时间顺序写，每一步：`作用简述` → `文件:行号` →（可选）短引用
- 项目内每一跳都要有位置；显而易见的标准库调用可跳过

## 不要求位置的情况

- 纯概念题、与仓库无关的通识说明
- 尚未定位到具体位置时，先说明「未在仓库中找到」，再说下一步怎么搜
